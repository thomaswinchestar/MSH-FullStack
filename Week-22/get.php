<?php
print_r($_GET); // Superglobal Variable
// URL Parse -> separtely and got to pieces and got the require one
// URL Decode -> URL Query value - space, special character if got browswser encode it and send to server, so we need to decode it to get the original value
// URL Encode -> "Space" %20, "@" %40, "#" %23, "$" %24, "%" %25, "&" %26, "+" %2B, "=" %3D, "?" %3F, "/" %2F, ":" %3A, ";" %3B, "@" %40, "<" %3C, ">" %3E, "|"
// URL Decode, String format to array
//PHP - URL Query -> $_GET
